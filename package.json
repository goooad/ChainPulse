{
  "name": "web3-sentry",
  "version": "1.0.0",
  "description": "Web3链上行为防火墙 + NFT情绪预测 + 主流币种波动监控",
  "main": "index.js",
  "scripts": {
    "dev": "concurrently \"npm run dev:frontend\" \"npm run dev:backend\"",
    "dev:frontend": "cd frontend && npm run dev",
    "dev:backend": "cd backend && npm run dev",
    "build": "npm run build:frontend && npm run build:backend && npm run build:extension",
    "build:frontend": "cd frontend && npm run build",
    "build:backend": "cd backend && npm run build",
    "build:extension": "cd extension && npm run build",
    "install:all": "npm install && cd frontend && npm install && cd ../backend && npm install && cd ../extension && npm install"
  },
  "keywords": ["web3", "security", "nft", "crypto", "ai", "blockchain"],
  "author": "Web3Sentry Team",
  "license": "MIT",
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}